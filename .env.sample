# Frontend Configuration
APP_PORT=5100
VITE_API_URL=http://localhost/api/

# Backend Configuration
# These IDs should match your host user's ID to avoid permission issues with mounted volumes.
# Run `id -u` and `id -g` in your terminal to find them.
USER_ID_TO_SAVE_FILES=1001
GROUP_ID_TO_SAVE_FILES=1001

# Backend Media Volume
# This is the path on the HOST machine where media files will be stored.
# Example: MEDIA_FOLDER_HOST_PATH=./media_files
MEDIA_FOLDER_HOST_PATH=

# Note: Database credentials are not configured here.
# The backend service reads them from a separate configuration file
# located at web/backend/backend/gnss_data.cfg, which should be
# created by copying web/backend/backend/conf_example.txt.

