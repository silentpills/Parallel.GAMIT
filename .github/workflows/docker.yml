name: Docker

on:
  push:
    branches: [main, master, dev]
    paths:
      - "web/backend/backend/Dockerfile"
      - "web/frontend/Dockerfile"
      - "docker-compose*.yml"
      - ".hadolint.yaml"
  pull_request:
    branches: [main, master, dev]
    paths:
      - "web/backend/backend/Dockerfile"
      - "web/frontend/Dockerfile"
      - "docker-compose*.yml"
      - ".hadolint.yaml"

permissions:
  contents: read

jobs:
  hadolint:
    name: Lint Dockerfiles
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Lint backend Dockerfile
        uses: hadolint/hadolint-action@v3.1.0
        with:
          dockerfile: web/backend/backend/Dockerfile

      - name: Lint frontend Dockerfile
        uses: hadolint/hadolint-action@v3.1.0
        with:
          dockerfile: web/frontend/Dockerfile

  trivy:
    name: Container Security Scan
    runs-on: ubuntu-latest
    needs: hadolint
    steps:
      - uses: actions/checkout@v4

      - name: Build backend image
        run: docker build -t gnss-backend ./web/backend/backend

      - name: Scan backend image
        uses: aquasecurity/trivy-action@0.28.0
        with:
          image-ref: gnss-backend
          format: table
          severity: HIGH,CRITICAL
          exit-code: 1

      - name: Build frontend image
        run: docker build -t gnss-frontend ./web/frontend
        env:
          VITE_API_URL: http://localhost

      - name: Scan frontend image
        uses: aquasecurity/trivy-action@0.28.0
        with:
          image-ref: gnss-frontend
          format: table
          severity: HIGH,CRITICAL
          exit-code: 1
